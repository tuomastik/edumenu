<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Edumenu.Styles">

    <!--Define reusable custom colors -->
    <!--When theme is changed, these values will be changed in code behind-->
    <SolidColorBrush Color="Salmon" x:Key="ThemeColor1" />
    <SolidColorBrush Color="IndianRed" x:Key="ThemeColor2" />
    <SolidColorBrush Color="WhiteSmoke" x:Key="ThemeColor3" />
    
    <Style x:Key="CampusHeaderTextBlock" TargetType="TextBlock">
        <Setter Property="FontSize" Value="60"/>
        <Setter Property="FontFamily" Value="Segoe WP Light"/>
        <Setter Property="Foreground" Value="#FF4D4D4D"/>
    </Style>
    <Style x:Key="RestaurantTextBlockTemplate" TargetType="TextBlock">
        <Setter Property="Margin" Value="0, 0, 0, 0"/>
        <Setter Property="FontSize" Value="32"/>
        <Setter Property="FontFamily" Value="Segoe WP"/>
        <Setter Property="Foreground" Value="White"/>
    </Style>
    <Style x:Key="HeaderBackgroundTemplate" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource ThemeColor2}" />
        <Setter Property="Margin" Value="0, 0, 0, 0"/>
    </Style>

</ResourceDictionary>
