<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--Define reusable custom colors -->
    <!--When theme is changed, these values will be changed in code behind-->
    <SolidColorBrush Color="Salmon" x:Key="ThemeColor1" />
    <SolidColorBrush Color="IndianRed" x:Key="ThemeColor2" />
    <SolidColorBrush Color="WhiteSmoke" x:Key="ThemeColor3" />
    <SolidColorBrush Color="CadetBlue" x:Key="ThemeColor4" />

    <SolidColorBrush Color="#ff2f313e" x:Key="ThemeColor5" />
    <SolidColorBrush Color="#FF242634" x:Key="ThemeColor6" />
    <SolidColorBrush Color="#FF181A25" x:Key="ThemeColor7" />

    <Style x:Key="CampusHeaderTextBlock" TargetType="TextBlock">
        <Setter Property="FontSize" Value="60"/>
        <Setter Property="FontFamily" Value="Segoe WP Light"/>
        <Setter Property="Foreground" Value="#FF4D4D4D"/>
    </Style>
    <Style x:Key="RestaurantTextBlockTemplate" TargetType="TextBlock">
        <Setter Property="Margin" Value="0, 0, 0, 0"/>
        <Setter Property="FontSize" Value="32"/>
        <Setter Property="FontFamily" Value="Segoe WP"/>
        <Setter Property="Foreground" Value="White"/>
    </Style>
    <Style x:Key="HeaderBackgroundTemplate" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource ThemeColor2}" />
        <Setter Property="Margin" Value="0, 0, 0, 0"/>
    </Style>




    <Style x:Key="ContentButton" TargetType="Button">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="MinHeight" Value="0"/>
        <Setter Property="MinWidth" Value="0"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <ContentPresenter Content="{TemplateBinding Content}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style x:Key="RestaurantButtonStyle" TargetType="Button" BasedOn="{StaticResource ContentButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid Background="{StaticResource ResourceKey=ThemeColor2}">
                        <ContentPresenter Content="{TemplateBinding Content}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



</ResourceDictionary>
